---
summary: "เอกสารอ้างอิงCLIสำหรับ `openclaw security` (ตรวจสอบและแก้ไขจุดเสี่ยงด้านความปลอดภัยที่พบบ่อย)"
read_when:
  - คุณต้องการรันการตรวจสอบความปลอดภัยอย่างรวดเร็วกับคอนฟิก/สถานะ
  - คุณต้องการนำคำแนะนำการ “แก้ไข” ที่ปลอดภัยไปใช้ (chmod, ทำให้ค่าเริ่มต้นรัดกุมขึ้น)
title: "security"
---

# `openclaw security`

เครื่องมือด้านความปลอดภัย(การตรวจสอบ + การแก้ไขตามตัวเลือก)

เกี่ยวข้อง:

- คู่มือความปลอดภัย: [Security](/gateway/security)

## การตรวจสอบ

```bash
openclaw security audit
openclaw security audit --deep
openclaw security audit --fix
```

การตรวจสอบจะเตือนเมื่อมีผู้ส่งDMหลายรายใช้เซสชันหลักร่วมกัน และแนะนำ **โหมดDMที่ปลอดภัย**: `session.dmScope="per-channel-peer"` (หรือ `per-account-channel-peer` สำหรับช่องทางแบบหลายบัญชี) สำหรับกล่องข้อความที่ใช้ร่วมกัน
นอกจากนี้ยังเตือนเมื่อใช้โมเดลขนาดเล็ก(`<=300B`)โดยไม่ใช้ sandboxing และเปิดใช้งานเครื่องมือเว็บ/เบราว์เซอร์อยู่
5.
นอกจากนี้ยังจะแจ้งเตือนเมื่อใช้โมเดลขนาดเล็ก (`<=300B`) โดยไม่เปิด sandbox และเปิดใช้เครื่องมือเว็บ/เบราว์เซอร์
สำหรับ webhook ingress จะแสดงคำเตือนเมื่อไม่ได้ตั้งค่า `hooks.defaultSessionKey`, เมื่อเปิดใช้งานการ override `sessionKey` ของคำขอ และเมื่อเปิดใช้งาน override โดยไม่ได้ตั้งค่า `hooks.allowedSessionKeyPrefixes`
นอกจากนี้ยังจะแสดงคำเตือนเมื่อมีการตั้งค่า sandbox Docker ขณะที่ปิดโหมด sandbox อยู่, เมื่อ `gateway.nodes.denyCommands` ใช้รูปแบบที่ไม่มีผลหรือไม่รู้จัก, เมื่อ `tools.profile="minimal"` ระดับโกลบอลถูกเขียนทับโดยโปรไฟล์เครื่องมือของเอเจนต์ และเมื่อเครื่องมือของปลั๊กอินส่วนขยายที่ติดตั้งไว้อาจเข้าถึงได้ภายใต้นโยบายเครื่องมือแบบผ่อนปรน
